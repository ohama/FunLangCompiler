---
phase: 02-scalar-codegen-via-mlirir
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/compiler/02-02-arith.flt
  - tests/compiler/02-03-let.flt
autonomous: true

must_haves:
  truths:
    - "An arithmetic expression '1 + 2 * 3 - 4 / 2' compiles and the binary exits with 5"
    - "A let-binding program 'let x = 5 in let y = x + 3 in y' compiles and the binary exits with 8"
    - "SSA names are unique (no redefinition errors from mlir-opt)"
    - "All FsLit tests in tests/compiler/ pass (01-return42, 02-01-literal, 02-02-arith, 02-03-let)"
  artifacts:
    - path: "tests/compiler/02-02-arith.flt"
      provides: "FsLit test for arithmetic expression end-to-end"
    - path: "tests/compiler/02-03-let.flt"
      provides: "FsLit test for let binding with variable reference end-to-end"
  key_links:
    - from: "tests/compiler/02-02-arith.flt"
      to: "Elaboration.elaborateExpr"
      via: "CLI parses arithmetic expr, elaboration emits ArithAddIOp/SubIOp/MulIOp/DivSIOp"
      pattern: "arith\\.(addi|subi|muli|divsi)"
    - from: "tests/compiler/02-03-let.flt"
      to: "Elaboration.elaborateExpr Let case"
      via: "CLI parses let expr, elaboration maps variable to SSA value in ElabEnv"
      pattern: "let.*in"
---

<objective>
Add FsLit end-to-end tests for arithmetic expressions and let/variable bindings, verifying that the Elaboration pass (built in 02-01) correctly handles the full Phase 2 scope.

Purpose: These tests exercise the arithmetic op elaboration (Add, Subtract, Multiply, Divide with correct precedence) and SSA variable binding (let x = ... in ... with variable lookup). Together with 02-01-literal.flt, they cover all four Phase 2 success criteria.

Output: Two new FsLit test files that pass end-to-end.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scalar-codegen-via-mlirir/02-RESEARCH.md
@tests/compiler/01-return42.flt
@tests/compiler/02-01-literal.flt
@src/FunLangCompiler.Compiler/Elaboration.fs
@src/FunLangCompiler.Cli/Program.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: FsLit tests for arithmetic and let bindings</name>
  <files>
    tests/compiler/02-02-arith.flt
    tests/compiler/02-03-let.flt
  </files>
  <action>
**02-02-arith.flt** — Create FsLit test for arithmetic expression. Use the exact same Command pattern as 01-return42.flt and 02-01-literal.flt:

```
// --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; echo $?; rm -f $OUTBIN'
// --- Input:
1 + 2 * 3 - 4 / 2
// --- Output:
5
```

The expression `1 + 2 * 3 - 4 / 2` follows standard operator precedence in FunLang:
- `2 * 3 = 6`
- `4 / 2 = 2`
- `1 + 6 - 2 = 5`

The result 5 is in the 0-255 exit code range.

**02-03-let.flt** — Create FsLit test for let bindings with variable references:

```
// --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; echo $?; rm -f $OUTBIN'
// --- Input:
let x = 5 in let y = x + 3 in y
// --- Output:
8
```

This tests:
- `let x = 5` — binds SSA value for constant 5 to variable "x" in ElabEnv
- `x + 3` — looks up "x" in ElabEnv, emits ArithAddIOp
- `let y = ... in y` — binds result, looks up "y"
- Result is 8 (= 5 + 3), in exit code range

These are the EXACT expressions from the Phase 2 success criteria in ROADMAP.md.
  </action>
  <verify>
Run all FsLit tests:

```bash
dotnet fslit tests/compiler/02-02-arith.flt
dotnet fslit tests/compiler/02-03-let.flt
```

Both must pass (exit code 0).

Also run full regression:
```bash
dotnet fslit tests/compiler/01-return42.flt
dotnet fslit tests/compiler/02-01-literal.flt
```

All four tests must pass.
  </verify>
  <done>
`02-02-arith.flt` passes — arithmetic expression `1 + 2 * 3 - 4 / 2` compiles and exits with 5.
`02-03-let.flt` passes — let binding `let x = 5 in let y = x + 3 in y` compiles and exits with 8.
All four FsLit tests in tests/compiler/ pass (no regressions).
  </done>
</task>

</tasks>

<verification>
1. `dotnet fslit tests/compiler/01-return42.flt` — passes
2. `dotnet fslit tests/compiler/02-01-literal.flt` — passes
3. `dotnet fslit tests/compiler/02-02-arith.flt` — passes (arithmetic expression = 5)
4. `dotnet fslit tests/compiler/02-03-let.flt` — passes (let binding = 8)
</verification>

<success_criteria>
- Arithmetic expression `1 + 2 * 3 - 4 / 2` compiles end-to-end and binary exits with 5
- Let binding `let x = 5 in let y = x + 3 in y` compiles end-to-end and binary exits with 8
- All four FsLit tests pass together (no regression)
- Phase 2 success criteria 2, 3, and 4 from ROADMAP.md are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-scalar-codegen-via-mlirir/02-02-SUMMARY.md`
</output>
