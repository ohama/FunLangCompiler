---
phase: 51-ast-structure-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FunLangCompiler.Compiler/Elaboration.fs
autonomous: true

must_haves:
  truths:
    - "dotnet build succeeds with zero errors and zero warnings on Elaboration.fs"
    - "All existing E2E tests (tests/compiler/*.flt) continue to pass"
    - "TypeDecl pattern matches 5 fields (name, typeParams, ctors, deriving, span)"
    - "TypeClassDecl, InstanceDecl, DerivingDecl are explicitly handled in prePassDecls"
  artifacts:
    - path: "src/FunLangCompiler.Compiler/Elaboration.fs"
      provides: "Updated pattern matches for LangThree v12.0 AST"
      contains: "TypeDecl(_, _, ctors, _, _)"
  key_links:
    - from: "src/FunLangCompiler.Compiler/Elaboration.fs"
      to: "LangThree/src/LangThree/Ast.fs"
      via: "pattern match on Ast.Decl.TypeDecl"
      pattern: "Ast\\.TypeDecl\\(_, _, ctors, _, _\\)"
---

<objective>
Fix the single compile error in Elaboration.fs caused by TypeDecl gaining a 5th field (deriving), and add explicit skip arms for the new TypeClassDecl, InstanceDecl, and DerivingDecl cases in prePassDecls.

Purpose: Unblock the build so FunLangCompiler compiles against the updated LangThree AST. This is the prerequisite for all subsequent v13.0 work.
Output: A compiling Elaboration.fs with all existing E2E tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-ast-structure-sync/51-RESEARCH.md
@src/FunLangCompiler.Compiler/Elaboration.fs (lines 4066-4102, prePassDecls function)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TypeDecl pattern and add explicit Decl arms</name>
  <files>src/FunLangCompiler.Compiler/Elaboration.fs</files>
  <action>
In the `prePassDecls` function (around line 4073), make these changes:

1. Update the TypeDecl pattern from 4-field to 5-field:
   BEFORE: `| Ast.Decl.TypeDecl (Ast.TypeDecl(_, _, ctors, _)) ->`
   AFTER:  `| Ast.Decl.TypeDecl (Ast.TypeDecl(_, _, ctors, _, _)) ->`
   The 4th wildcard captures `deriving: string list`, the 5th captures `Span`.

2. Add explicit skip arms for the three new Decl cases BEFORE the existing `| _ -> ()` wildcard (around line 4101):
   ```fsharp
   | Ast.Decl.TypeClassDecl _ -> ()   // Phase 52: typeclasses handled in elaborateTypeclasses
   | Ast.Decl.InstanceDecl _ -> ()    // Phase 52: instances handled in elaborateTypeclasses
   | Ast.Decl.DerivingDecl _ -> ()    // Phase 52: deriving handled in elaborateTypeclasses
   | _ -> ()
   ```

Do NOT modify any other function (flattenDecls, extractMainExpr, etc.) -- they already handle new cases via wildcard arms.
Do NOT modify any LangThree files.
  </action>
  <verify>
1. `dotnet build src/FunLangCompiler.Compiler/FunLangCompiler.Compiler.fsproj` succeeds with 0 errors and 0 warnings
2. Run a representative sample of E2E tests to confirm no regressions:
   - `tests/compiler/01-return42.flt` (basic)
   - `tests/compiler/35-08-list-tryfind-choose.flt` (complex, recently modified)
   - `tests/compiler/46-01-record-type-hint.flt` (error messages)
   Run each by extracting and executing the Command line from the .flt file.
  </verify>
  <done>
- `dotnet build` exits 0 with no errors or warnings related to Elaboration.fs
- E2E test sample produces expected output matching .flt CHECK lines
- The pattern `TypeDecl(_, _, ctors, _, _)` exists in Elaboration.fs
- Explicit arms for TypeClassDecl, InstanceDecl, DerivingDecl exist in prePassDecls
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/FunLangCompiler.Compiler/FunLangCompiler.Compiler.fsproj` -- must exit 0, no FS0727 error
2. Run at least 3 E2E tests spanning different test categories to confirm no regressions
3. Grep Elaboration.fs for `TypeDecl(_, _, ctors, _, _)` -- must find exactly 1 match
4. Grep Elaboration.fs for `TypeClassDecl _` -- must find at least 1 match (the new explicit arm)
</verification>

<success_criteria>
- Build succeeds: `dotnet build` exits 0 with no errors
- TypeDecl uses 5-field pattern
- TypeClassDecl, InstanceDecl, DerivingDecl have explicit skip arms in prePassDecls
- All tested E2E tests produce correct output
</success_criteria>

<output>
After completion, create `.planning/phases/51-ast-structure-sync/51-01-SUMMARY.md`
</output>
