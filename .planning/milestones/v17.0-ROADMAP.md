# Milestone v17.0: Project File (funproj.toml)

**Status:** ✅ SHIPPED 2026-04-01
**Phases:** 60-61
**Total Plans:** 3

## Overview

FunLang과 동일한 funproj.toml로 멀티파일 프로젝트를 네이티브 바이너리로 컴파일. `fnc build`/`fnc test` CLI 서브커맨드 추가.

## Phases

### Phase 60: funproj.toml 파서

**Goal**: FunLang 호환 funproj.toml 파싱
**Plans**: 1 plan

Plans:
- [x] 60-01: TOML subset parser (ProjectFile.fs) + E2E test

**Details:**
- ProjectFile.fs: Target, FunProjConfig 타입, parseFunProj, findFunProj, resolveTarget
- 최소 TOML 서브셋 파서 (외부 의존성 없음)
- tests/projfile/ E2E 테스트 프로젝트

### Phase 61: fnc build/test 서브커맨드

**Goal**: CLI 서브커맨드로 프로젝트 단위 컴파일/실행
**Plans**: 2 plans

Plans:
- [x] 61-01: Extract compileFile helper + wire build/test subcommands
- [x] 61-02: E2E tests for fnc build and fnc test

**Details:**
- compileFile 헬퍼: Prelude 로딩 → 파싱 → import 확장 → elaboration → Pipeline.compile
- handleBuild: funproj.toml [[executable]] 타겟 → build/ 네이티브 바이너리
- handleTest: funproj.toml [[test]] 타겟 → 컴파일 + 실행 + PASS/FAIL 리포트
- 5개 E2E 테스트: full build, specific target, test, no-funproj error, bad-target error

---

## Milestone Summary

**Key Decisions:**
- No external TOML library — hand-rolled minimal parser
- Standalone test project (tests/projfile/) for parser tests
- compileFile helper with optional preludeDir parameter
- fnc test continues on compilation failure (reports all results)

**Issues Resolved:**
None.

**Technical Debt Incurred:**
None.

---
*Archived: 2026-04-01*
