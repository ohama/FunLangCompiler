---
phase: 12-missing-operators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/LangBackend.Compiler/MlirIR.fs
  - src/LangBackend.Compiler/Printer.fs
  - src/LangBackend.Compiler/Elaboration.fs
autonomous: true

must_haves:
  truths:
    - "10 % 3 compiles and exits with code 1"
    - "'A' compiles and exits with code 65"
    - "Modulo inside a lambda closure captures free variables correctly"
    - "App(Lambda(...), arg) inline call works (prerequisite for Plan 02 PipeRight)"
  artifacts:
    - src/LangBackend.Compiler/MlirIR.fs contains ArithRemSIOp
    - src/LangBackend.Compiler/Printer.fs emits arith.remsi
    - src/LangBackend.Compiler/Elaboration.fs has Modulo, Char, and App(Lambda) cases
  key_links:
    - ArithRemSIOp in MlirIR.fs is printed as "arith.remsi" by Printer.fs
    - Modulo elaboration uses ArithRemSIOp identically to how Divide uses ArithDivSIOp
    - Char elaboration uses int64 (int c) to get the Unicode code point
---

<objective>
Add Modulo operator (%) and Char literal ('A') to the compiler, plus fix App(Lambda) inline application which is a prerequisite for Plan 02's PipeRight.

Purpose: `10 % 3 = 1` and `'A' = 65` are the two simplest missing operators. The App(Lambda) fix unblocks inline lambda calls used by PipeRight.
Output: ArithRemSIOp in the IR, arith.remsi in printed MLIR, Modulo/Char/App(Lambda) cases in elaboration, freeVars updated for Modulo.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-missing-operators/12-RESEARCH.md
@src/LangBackend.Compiler/MlirIR.fs
@src/LangBackend.Compiler/Printer.fs
@src/LangBackend.Compiler/Elaboration.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ArithRemSIOp to MlirIR and Printer</name>
  <files>src/LangBackend.Compiler/MlirIR.fs, src/LangBackend.Compiler/Printer.fs</files>
  <action>
In MlirIR.fs, add one line after the ArithDivSIOp case (line 44):
    | ArithRemSIOp    of result: MlirValue * lhs: MlirValue * rhs: MlirValue

In Printer.fs, add one case after the ArithDivSIOp print case (after line 42):
    | ArithRemSIOp(result, lhs, rhs) ->
        sprintf "%s%s = arith.remsi %s, %s : %s"
            indent result.Name lhs.Name rhs.Name (printType result.Type)

That is all. No other files change in this task.
  </action>
  <verify>dotnet build src/LangBackend.Compiler/LangBackend.Compiler.fsproj 2>&1 | grep -E "error|warning|Build succeeded"</verify>
  <done>Build succeeds with no errors. ArithRemSIOp is a valid MlirOp DU case. Printer has a non-exhaustive match warning gone.</done>
</task>

<task type="auto">
  <name>Task 2: Add Modulo, Char, App(Lambda), and freeVars cases to Elaboration.fs</name>
  <files>src/LangBackend.Compiler/Elaboration.fs</files>
  <action>
Make four additions to Elaboration.fs:

**A. freeVars: add Modulo and pipe/compose cases** (before the catch-all `| _ -> Set.empty` at approximately line 109):
    | Modulo (l, r, _) ->
        Set.union (freeVars boundVars l) (freeVars boundVars r)
    | PipeRight (l, r, _) | ComposeRight (l, r, _) | ComposeLeft (l, r, _) ->
        Set.union (freeVars boundVars l) (freeVars boundVars r)

**B. elaborateExpr: Modulo case** — add after the Divide case (after line 257):
    | Modulo (lhs, rhs, _) ->
        let (lv, lops) = elaborateExpr env lhs
        let (rv, rops) = elaborateExpr env rhs
        let result = { Name = freshName env; Type = I64 }
        (result, lops @ rops @ [ArithRemSIOp(result, lv, rv)])

**C. elaborateExpr: Char case** — add after the Number case (after line 237):
    | Char (c, _) ->
        let v = { Name = freshName env; Type = I64 }
        (v, [ArithConstantOp(v, int64 (int c))])

**D. elaborateExpr: App(Lambda) inline case** — add inside the App handler BEFORE the general `| _ -> failwithf` branch. The App handler at line 683 dispatches on `funcExpr`. Add a new `| Lambda(param, body, _)` arm inside that match:
    | Lambda(param, body, _) ->
        // Inline lambda application: (fun x -> body) arg  ≡  let x = arg in body
        let (argVal, argOps) = elaborateExpr env argExpr
        let env' = { env with Vars = Map.add param argVal env.Vars }
        let (bodyVal, bodyOps) = elaborateExpr env' body
        (bodyVal, argOps @ bodyOps)

This must appear in the `match funcExpr with` block inside `App`, before the `| _ -> failwithf` fallthrough.
  </action>
  <verify>
1. dotnet build 2>&1 | grep -E "error|Build succeeded"
2. Write test file /tmp/test_modulo.lang: `let main = 10 % 3` — run through CLI and check exit code 1
3. Write test file /tmp/test_char.lang: `let main = 'A'` — run through CLI and check exit code 65
  </verify>
  <done>
Build succeeds. `10 % 3` exits 1. `'A'` exits 65. freeVars no longer has a catch-all gap for Modulo.
  </done>
</task>

</tasks>

<verification>
Run the two smoke tests:
```bash
# Modulo: 10 % 3 = 1
echo "let main = 10 % 3" > /tmp/test_modulo.lang
dotnet run --project src/LangBackend.Cli -- /tmp/test_modulo.lang /tmp/test_modulo
/tmp/test_modulo; echo "Exit: $?"
# Expected: Exit: 1

# Char: 'A' = 65
echo "let main = 'A'" > /tmp/test_char.lang
dotnet run --project src/LangBackend.Cli -- /tmp/test_char.lang /tmp/test_char
/tmp/test_char; echo "Exit: $?"
# Expected: Exit: 65
```
</verification>

<success_criteria>
- `10 % 3` compiles and process exits with code 1 (OP-01)
- `'A'` compiles and process exits with code 65 (OP-02)
- `dotnet build` reports 0 errors
- ArithRemSIOp case exists in MlirIR.fs
- arith.remsi emitted in Printer.fs
- App(Lambda) inline path works (verified by running `(fun x -> x + 1) 5` → exit 6)
</success_criteria>

<output>
After completion, create `.planning/phases/12-missing-operators/12-01-SUMMARY.md`
</output>
