---
phase: 20-completeness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FunLangCompiler.Compiler/Elaboration.fs
  - tests/e2e/20-02-nested-adt-pat.flt
  - tests/e2e/20-03-raise-in-handler.flt
autonomous: true

must_haves:
  truths:
    - "Nested ADT pattern like Node(Node(Leaf, v, Leaf), root, _) correctly extracts values at depth 2+"
    - "Exception raised inside a handler arm body propagates to outer handler correctly"
    - "EXN-07 (handler miss re-raise) continues to work — test 19-06 still passes"
    - "All 63 existing E2E tests continue to pass"
  artifacts:
    - path: "src/FunLangCompiler.Compiler/Elaboration.fs"
      provides: "Ptr pre-load in ensureAdtFieldTypes + conditional merge branch in emitDecisionTree Leaf/Guard"
      contains: "resolveAccessorTyped.*Ptr"
    - path: "tests/e2e/20-02-nested-adt-pat.flt"
      provides: "E2E test for nested ADT pattern matching"
    - path: "tests/e2e/20-03-raise-in-handler.flt"
      provides: "E2E test for raise inside handler arm"
  key_links:
    - from: "ensureAdtFieldTypes"
      to: "resolveAccessorTyped"
      via: "Pre-load payload as Ptr instead of I64"
      pattern: "resolveAccessorTyped.*Ptr"
    - from: "emitDecisionTree Leaf case"
      to: "CfBrOp(mergeLabel)"
      via: "Only append merge branch if body not already terminated"
      pattern: "List\\.tryLast.*LlvmUnreachableOp"
---

<objective>
Fix nested ADT pattern matching (ADT-12) and raise-inside-handler (EXN-08).

Purpose: Two related bugs in the pattern matching emission code. (1) Nested ADT patterns fail because `ensureAdtFieldTypes` pre-loads payload as I64 but nested ADT payloads are Ptr. (2) Raise inside a handler arm body appends CfBrOp after LlvmUnreachableOp, violating MLIR block structure. Both bugs exist in the Match code AND the TryWith duplicate code (suffix `2` functions).

Output: Fixed `ensureAdtFieldTypes`/`ensureAdtFieldTypes2` and `emitDecisionTree`/`emitDecisionTree2` Leaf+Guard cases, plus two E2E tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-completeness/20-RESEARCH.md
@src/FunLangCompiler.Compiler/Elaboration.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ensureAdtFieldTypes pre-load type + emitDecisionTree terminator check</name>
  <files>src/FunLangCompiler.Compiler/Elaboration.fs</files>
  <action>
Apply four micro-fixes in Elaboration.fs. Each fix must be applied to BOTH the Match version AND the TryWith duplicate (suffix `2`) version.

**Fix A — ensureAdtFieldTypes (ADT-12):**
Find `ensureAdtFieldTypes` (around line 1122). It pre-loads ADT payload at slot 1. Change the pre-load from I64 to Ptr:
- Find where it calls `resolveAccessor argAccs.[0]` or `resolveAccessorTyped argAccs.[0] I64`
- Change to `resolveAccessorTyped argAccs.[0] Ptr`
- Rationale: Nested ADT payloads are pointers to heap blocks. For scalar (i64) payloads, `resolveAccessorTyped` will re-load as I64 when the pattern variable is bound — the cache retype mechanism handles this.

Apply the SAME fix to `ensureAdtFieldTypes2` in the TryWith duplicate section (around lines 1591-1810).

**Fix B — emitDecisionTree Leaf case (EXN-08):**
Find the Leaf case in `emitDecisionTree` (around line 1165). Currently it unconditionally appends `CfBrOp(mergeLabel, [bodyVal])` after `bodyOps`. Change to conditional:
```fsharp
let terminatedOps =
    match List.tryLast bodyOps with
    | Some LlvmUnreachableOp -> bodyOps
    | Some (CfBrOp _) | Some (CfCondBrOp _) -> bodyOps
    | _ -> bodyOps @ [CfBrOp(mergeLabel, [bodyVal])]
```
Use `terminatedOps` instead of the unconditional `bodyOps @ [CfBrOp(...)]`.

Apply the SAME fix to the Leaf case in `emitDecisionTree2` (TryWith duplicate).

**Fix C — emitDecisionTree Guard case (EXN-08):**
Find the Guard case in `emitDecisionTree` (around line 1210-1240). It has the same pattern — body block appends CfBrOp unconditionally. Apply the same conditional terminator check as Fix B.

Apply the SAME fix to the Guard case in `emitDecisionTree2` (TryWith duplicate).

IMPORTANT: Do NOT modify the `exn_fail` block (EXN-07 is already working). Do NOT change `resolveAccessor`/`resolveAccessorTyped` general logic. Do NOT touch MatchCompiler.fs.

Total changes: ~6 edit sites (3 fixes x 2 copies each), each 3-5 lines.
  </action>
  <verify>
Run `dotnet build` — must compile without errors.
Run existing test suite — all 63 tests must pass, especially 19-06-try-fallthrough.flt (EXN-07 regression gate).
  </verify>
  <done>ensureAdtFieldTypes/2 pre-load Ptr, emitDecisionTree/2 Leaf+Guard cases conditionally skip merge branch when body is noreturn.</done>
</task>

<task type="auto">
  <name>Task 2: E2E tests for nested ADT patterns and raise-in-handler</name>
  <files>tests/e2e/20-02-nested-adt-pat.flt, tests/e2e/20-03-raise-in-handler.flt</files>
  <action>
Create two E2E test files following existing FsLit conventions (check nearby tests for exact format — no trailing newline).

**Test 1: `tests/e2e/20-02-nested-adt-pat.flt`** (ADT-12)
```
type Tree = Leaf | Node of Tree * int * Tree
let inner = Node(Leaf, 5, Leaf) in
let outer = Node(inner, 10, Leaf) in
match outer with
| Node(Node(_, v, _), root, _) -> root + v
| _ -> 0
```
Expected exit code: 15 (10 + 5). Verifies nested ADT constructor pattern extracts values at depth 2.

**Test 2: `tests/e2e/20-03-raise-in-handler.flt`** (EXN-08)
```
exception Failure of string
exception NotFound

let result =
  try
    try raise NotFound
    with
    | NotFound -> raise (Failure "from handler")
  with
  | Failure msg -> 42

result
```
Expected exit code: 42. Verifies that `raise` inside a handler arm body propagates to the outer handler correctly.

Run the full test suite including both new tests. Confirm all 65 tests pass (63 existing + 2 new).
  </action>
  <verify>
Run `dotnet test` or the project's E2E test runner.
- 20-02 must produce exit code 15
- 20-03 must produce exit code 42
- All 65 tests pass (63 existing + 2 new)
  </verify>
  <done>Both new E2E tests pass, proving nested ADT patterns and raise-in-handler work correctly. Total test count: 65.</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- All 65 tests pass (63 existing + 2 new)
- Test 20-02 exits with 15 (nested ADT pattern extraction)
- Test 20-03 exits with 42 (raise inside handler propagates to outer)
- Test 19-06 still passes (EXN-07 regression gate)
- Existing ADT tests (17-01 through 17-06) still pass (no regressions from Ptr pre-load change)
</verification>

<success_criteria>
- Nested constructor pattern `Node(Node(_, v, _), root, _)` extracts values at depth 2+
- Raise inside handler arm compiles to valid MLIR and propagates exception correctly
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/20-completeness/20-02-SUMMARY.md`
</output>
