---
phase: 20-completeness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FunLangCompiler.Compiler/Elaboration.fs
  - tests/compiler/20-04-ho-ctor.flt
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "apply Some 42 compiles and exits with 42 — constructor closure passed as higher-order function argument works"
    - "All 67 E2E tests pass (66 existing + 1 new)"
  artifacts:
    - path: "src/FunLangCompiler.Compiler/Elaboration.fs"
      provides: "I64-typed closure dispatch via inttoptr in App case"
      contains: "closureVal.Type = I64"
    - path: "tests/compiler/20-04-ho-ctor.flt"
      provides: "E2E test for higher-order constructor passing"
      contains: "apply"
  key_links:
    - from: "App case Var(name) branch"
      to: "IndirectCallOp"
      via: "inttoptr on I64-typed closure value before GEP/load/call"
      pattern: "LlvmIntToPtrOp.*closureVal"
---

<objective>
Fix higher-order constructor passing so that constructor closures passed as function arguments can be called at the callee site.

Purpose: Close the one remaining gap in Phase 20 verification — `apply Some 42` (and by extension `List.map Some [1;2;3]`) must compile and run correctly. Currently fails because lambda parameters are typed I64 and the App dispatch only recognizes Ptr-typed values as closures.

Output: Patched Elaboration.fs + new E2E test proving the fix.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-completeness/20-01-SUMMARY.md
@.planning/phases/20-completeness/20-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add I64 closure dispatch path in App case</name>
  <files>src/FunLangCompiler.Compiler/Elaboration.fs</files>
  <action>
In the App case for `Var(name, _)` (around line 844-856), add a new match arm after the existing `closureVal.Type = Ptr` arm. The new arm handles `closureVal.Type = I64`:

```
| Some closureVal when closureVal.Type = I64 ->
    // I64 value may be a closure passed through uniform ABI — cast to Ptr and attempt indirect call
    let (argVal, argOps) = elaborateExpr env argExpr
    let closurePtrVal = { Name = freshName env; Type = Ptr }
    let fnPtrVal = { Name = freshName env; Type = Ptr }
    let result = { Name = freshName env; Type = I64 }
    let castOp = LlvmIntToPtrOp(closurePtrVal, closureVal)
    let loadOp = LlvmLoadOp(fnPtrVal, closurePtrVal)
    let callOp = IndirectCallOp(result, fnPtrVal, closurePtrVal, argVal)
    (result, argOps @ [castOp; loadOp; callOp])
```

This mirrors the existing Ptr path (lines 847-854) but prepends an `inttoptr` cast. The `LlvmIntToPtrOp` DU case already exists in MlirIR.fs (added in 20-01). No changes needed to MlirIR.fs or Printer.fs.

Place this arm between the `closureVal.Type = Ptr` arm and the `| _ ->` fallthrough. The fallthrough `failwithf` should remain as a catch-all for unexpected types.

IMPORTANT: Do NOT change the existing Ptr arm. Only ADD a new arm for I64.
  </action>
  <verify>
Run `dotnet build src/FunLangCompiler.Compiler/FunLangCompiler.Compiler.fsproj` — must compile with no errors.
Run existing 66 tests: `dotnet run --project tests/FsLitRunner/FsLitRunner.fsproj -- tests/compiler/` — all 66 must pass.
  </verify>
  <done>Elaboration.fs compiles. All 66 existing tests pass (no regressions). The App case now handles both Ptr and I64 closure values.</done>
</task>

<task type="auto">
  <name>Task 2: E2E test for higher-order constructor passing</name>
  <files>tests/compiler/20-04-ho-ctor.flt</files>
  <action>
Create `tests/compiler/20-04-ho-ctor.flt` with this content:

```
// --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; echo $?; rm -f $OUTBIN'
// --- Input:
type Option = None | Some of int
let apply f x = f x in
match apply Some 42 with
| Some n -> n
| None -> 0
// --- Output:
42
```

This test passes constructor `Some` as an argument to `apply`, which then calls `f x` where `f` has type I64 (lambda parameter ABI). The expected exit code is 42 (the value wrapped in Some, extracted by pattern match).

Run the full test suite after creating: `dotnet run --project tests/FsLitRunner/FsLitRunner.fsproj -- tests/compiler/` — all 67 tests (66 existing + 1 new) must pass.
  </action>
  <verify>
`dotnet run --project tests/FsLitRunner/FsLitRunner.fsproj -- tests/compiler/` reports 67/67 passed.
  </verify>
  <done>`apply Some 42` compiles and exits 42. All 67 tests pass. ADT-11 fully satisfied.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/FunLangCompiler.Compiler/FunLangCompiler.Compiler.fsproj` compiles cleanly
2. `dotnet run --project tests/FsLitRunner/FsLitRunner.fsproj -- tests/compiler/` reports 67/67 passed
3. The new test `20-04-ho-ctor.flt` specifically tests `apply Some 42` (higher-order constructor passing)
4. No regressions in existing 66 tests
</verification>

<success_criteria>
- `apply Some 42` compiles and produces exit code 42
- All 67 E2E tests pass (66 existing + 1 new)
- ADT-11 requirement fully satisfied (constructor as first-class function argument, not just let-binding)
</success_criteria>

<output>
After completion, create `.planning/phases/20-completeness/20-03-SUMMARY.md`
</output>
