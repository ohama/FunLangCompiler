---
phase: 28-syntax-desugaring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/compiler/28-01-seq-basic.flt
  - tests/compiler/28-02-seq-chain.flt
  - tests/compiler/28-03-seq-side-effect.flt
  - tests/compiler/28-04-ite-basic.flt
  - tests/compiler/28-05-ite-unit.flt
autonomous: true

must_haves:
  truths:
    - "e1; e2 evaluates e1 for side effects then returns e2"
    - "e1; e2; e3 chains correctly (right-associative)"
    - "if cond then expr without else compiles and returns unit when false"
  artifacts:
    - path: "tests/compiler/28-01-seq-basic.flt"
      provides: "E2E test for basic semicolon sequencing"
    - path: "tests/compiler/28-02-seq-chain.flt"
      provides: "E2E test for chained semicolon sequencing"
    - path: "tests/compiler/28-03-seq-side-effect.flt"
      provides: "E2E test for side effects in sequencing"
    - path: "tests/compiler/28-04-ite-basic.flt"
      provides: "E2E test for if-then without else"
    - path: "tests/compiler/28-05-ite-unit.flt"
      provides: "E2E test for if-then returning unit"
  key_links:
    - from: "LangThree parser"
      to: "LetPat(WildcardPat, e1, e2)"
      via: "semicolon desugaring in parser"
      pattern: "LetPat.*WildcardPat"
    - from: "LangThree parser"
      to: "If(cond, expr, Tuple([]))"
      via: "if-then desugaring in parser"
      pattern: "If.*Tuple"
---

<objective>
Verify that expression sequencing (;) and if-then-without-else already work via existing LangThree parser desugaring + backend elaboration, by adding E2E FsLit tests.

Purpose: SEQ and ITE are desugared by the LangThree parser before reaching the backend. `e1; e2` becomes `LetPat(WildcardPat, e1, e2)` and `if cond then expr` becomes `If(cond, expr, Tuple([]))`. The backend already handles both LetPat+WildcardPat (Elaboration.fs line 538) and If+Tuple (lines 652, 1314). No code changes are expected — only E2E tests to confirm and document this behavior.

Output: 5 FsLit E2E test files covering SEQ-01, SEQ-02, ITE-01, ITE-02
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/FunLangCompiler.Compiler/Elaboration.fs (line 538: LetPat WildcardPat handler, line 652: If handler, line 1314: Tuple handler)
@tests/compiler/07-02-print-basic.flt (test format reference)
@tests/compiler/22-02-array-get-set.flt (test format reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SEQ E2E tests (SEQ-01, SEQ-02)</name>
  <files>
    tests/compiler/28-01-seq-basic.flt
    tests/compiler/28-02-seq-chain.flt
    tests/compiler/28-03-seq-side-effect.flt
  </files>
  <action>
Create three FsLit test files using the standard test format (see 07-02-print-basic.flt for reference).

**28-01-seq-basic.flt** (SEQ-01 — basic sequencing):
```
// --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; echo $?; rm -f $OUTBIN'
// --- Input:
let x = 10 in
x; 42
// --- Output:
42
```
Tests that `e1; e2` returns e2's value, discarding e1.

**28-02-seq-chain.flt** (SEQ-02 — right-associative chaining):
```
// --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; echo $?; rm -f $OUTBIN'
// --- Input:
1; 2; 3
// --- Output:
3
```
Tests that `e1; e2; e3` chains correctly — returns last expression.

**28-03-seq-side-effect.flt** (SEQ-01 — side effects execute):
```
// --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; echo $?; rm -f $OUTBIN'
// --- Input:
print "hello"; print " world"; 0
// --- Output:
hello world0
```
Tests that side effects from e1 and e2 execute in order before returning e3.
  </action>
  <verify>
Run: `dotnet test` (or the FsLit test runner) and confirm all three new tests pass.
Also run the full test suite to verify no regressions.
  </verify>
  <done>All three SEQ tests pass. 118 existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add ITE E2E tests (ITE-01, ITE-02)</name>
  <files>
    tests/compiler/28-04-ite-basic.flt
    tests/compiler/28-05-ite-unit.flt
  </files>
  <action>
Create two FsLit test files for if-then-without-else.

**28-04-ite-basic.flt** (ITE-01 — if-then without else, true branch):
```
// --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; echo $?; rm -f $OUTBIN'
// --- Input:
if true then print "yes";
0
// --- Output:
yes0
```
Tests that `if true then expr` executes the then-branch side effect.

**28-05-ite-unit.flt** (ITE-02 — if-then without else, false branch returns unit):
```
// --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; echo $?; rm -f $OUTBIN'
// --- Input:
if false then print "no";
42
// --- Output:
42
```
Tests that `if false then expr` does NOT execute the then-branch and the program continues normally.

NOTE: The semicolon after the if-then is the sequencing operator (;), which itself desugars to LetPat(WildcardPat, ...). The if-then expression returns unit (Tuple([])) when the condition is false. The overall pattern is: `(if cond then sideEffect); resultExpr`.
  </action>
  <verify>
Run: `dotnet test` and confirm both ITE tests pass.
Full suite: all 118 + 5 new tests pass (123 total).
  </verify>
  <done>Both ITE tests pass. Total test count is 123 (118 existing + 5 new).</done>
</task>

</tasks>

<verification>
1. `dotnet test` — all tests pass (118 existing + 5 new = 123)
2. No code changes to Elaboration.fs or any other source file — SEQ and ITE work purely through existing parser desugaring + backend elaboration
</verification>

<success_criteria>
- 5 new FsLit E2E tests added and passing
- No source code changes needed (only test files)
- Requirements SEQ-01, SEQ-02, ITE-01, ITE-02 verified
- All 118 existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/28-syntax-desugaring/28-01-SUMMARY.md`
</output>
