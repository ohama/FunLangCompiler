---
phase: 30-annotations-and-for-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FunLangCompiler.Compiler/Elaboration.fs
  - tests/compiler/30-01-annot-basic.flt
  - tests/compiler/30-02-annot-lambda.flt
autonomous: true

must_haves:
  truths:
    - "(e : T) compiles and produces the same output as e alone"
    - "fun (x: T) -> body compiles identically to fun x -> body"
    - "freeVars correctly captures variables through Annot and LambdaAnnot nodes"
  artifacts:
    - path: "src/FunLangCompiler.Compiler/Elaboration.fs"
      provides: "Annot, LambdaAnnot, freeVars cases"
      contains: "| Annot"
    - path: "tests/compiler/30-01-annot-basic.flt"
      provides: "E2E test for type annotation expression"
    - path: "tests/compiler/30-02-annot-lambda.flt"
      provides: "E2E test for annotated lambda"
  key_links:
    - from: "Elaboration.fs elaborateExpr"
      to: "Annot case"
      via: "pattern match delegates to inner expr"
      pattern: "Annot.*elaborateExpr"
    - from: "Elaboration.fs freeVars"
      to: "Annot/LambdaAnnot cases"
      via: "recursive freeVars on inner expr/body"
      pattern: "Annot.*freeVars"
---

<objective>
Add type annotation pass-through to the compiler: Annot(expr, _, _) and LambdaAnnot(param, _, body, _) are handled in both elaborateExpr and freeVars. The compiler ignores type annotations at codegen, passing through to the inner expression.

Purpose: ANN-01, ANN-02, ANN-03 — type annotations must compile without error
Output: 2 new match cases in elaborateExpr, 2 new cases in freeVars, 2 E2E tests
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/FunLangCompiler.Compiler/Elaboration.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Annot/LambdaAnnot to freeVars and elaborateExpr</name>
  <files>src/FunLangCompiler.Compiler/Elaboration.fs</files>
  <action>
  1. In the `freeVars` function (around line 170, after the ForExpr case), add two new cases:

     ```fsharp
     | Annot (expr, _, _) -> freeVars boundVars expr
     | LambdaAnnot (param, _, body, _) -> freeVars (Set.add param boundVars) body
     ```

     LambdaAnnot binds `param` just like Lambda does, so add param to boundVars before recursing on body.

  2. In the `elaborateExpr` function, add two new match cases. Place them near the existing Lambda case or at the end before the catch-all. The cases are trivial pass-throughs:

     ```fsharp
     // Phase 30: Type annotation pass-through — ignore type at codegen
     | Annot (expr, _, _) -> elaborateExpr env expr
     | LambdaAnnot (param, _, body, span) -> elaborateExpr env (Lambda(param, body, span))
     ```

     Annot simply delegates to the inner expression. LambdaAnnot rewrites to an equivalent Lambda and re-elaborates.
  </action>
  <verify>
  Run `dotnet build src/FunLangCompiler.Compiler/FunLangCompiler.Compiler.fsproj` — must compile with no errors.
  </verify>
  <done>Annot and LambdaAnnot cases exist in both freeVars and elaborateExpr, project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Add E2E tests for annotations</name>
  <files>tests/compiler/30-01-annot-basic.flt, tests/compiler/30-02-annot-lambda.flt</files>
  <action>
  Create two E2E test files following the existing .flt format (see tests/compiler/29-01-while-basic.flt for template).

  **30-01-annot-basic.flt** — tests that `(e : T)` compiles and produces the same result as `e`:
  ```
  // --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; echo $?; rm -f $OUTBIN'
  // --- Input:
  let x = (42 : int) in
  print (to_string x)
  // --- Output:
  420
  ```

  **30-02-annot-lambda.flt** — tests that `fun (x: T) -> body` works like `fun x -> body`:
  ```
  // --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; echo $?; rm -f $OUTBIN'
  // --- Input:
  let f = fun (x : int) -> x + 1 in
  print (to_string (f 10))
  // --- Output:
  110
  ```

  Then run all 140 tests (138 existing + 2 new): `dotnet test` or the project's test runner. Confirm all pass.
  Run: `cd tests/compiler && for f in *.flt; do ... done` or use the project's test harness.
  </action>
  <verify>
  Run all E2E tests. All 140 tests must pass (138 existing + 2 new).
  </verify>
  <done>140 E2E tests pass, including the 2 new annotation tests. REG-01 holds.</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- All 140 E2E tests pass (138 existing + 2 new)
- `(42 : int)` produces same output as `42`
- `fun (x : int) -> x + 1` produces same output as `fun x -> x + 1`
</verification>

<success_criteria>
- ANN-01: Annot pass-through compiles and runs correctly
- ANN-02: LambdaAnnot pass-through compiles and runs correctly
- ANN-03: freeVars handles Annot/LambdaAnnot for correct closure capture
- REG-01: All 138 prior tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-annotations-and-for-in/30-01-SUMMARY.md`
</output>
