---
phase: 20-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/LangBackend.Compiler/Elaboration.fs
  - tests/e2e/20-01-firstclass-ctor.flt
autonomous: true

must_haves:
  truths:
    - "Unary constructor used as a value (e.g., `apply Some 42`) compiles to a closure and produces correct results at runtime"
    - "Nullary constructors (e.g., `None`, `Leaf`) continue to allocate 16-byte blocks as before"
    - "All 63 existing E2E tests continue to pass"
  artifacts:
    - path: "src/LangBackend.Compiler/Elaboration.fs"
      provides: "Arity-aware Constructor(name, None, _) case"
      contains: "info.Arity"
    - path: "tests/e2e/20-01-firstclass-ctor.flt"
      provides: "E2E test for first-class unary constructor"
  key_links:
    - from: "Elaboration.fs Constructor(name, None, _) case"
      to: "env.TypeEnv"
      via: "Map.find name env.TypeEnv to check Arity"
      pattern: "Map\\.find.*TypeEnv"
    - from: "Elaboration.fs Constructor(name, None, _) arity>=1 branch"
      to: "Lambda elaboration path"
      via: "Re-elaborate as Lambda wrapping the constructor"
      pattern: "Lambda.*Constructor"
---

<objective>
Make unary ADT constructors work as first-class values (ADT-11).

Purpose: When `Some` appears without its argument (e.g., `apply Some 42`, `map Some xs`), it must produce a closure `fun x -> Some x`, not a nullary allocation block. Currently `Constructor(name, None, _)` always allocates a nullary 16-byte block regardless of arity.

Output: Modified `Constructor(name, None, _)` case in `elaborateExpr` that checks TypeEnv arity, plus one E2E test.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-completeness/20-RESEARCH.md
@src/LangBackend.Compiler/Elaboration.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Arity-aware Constructor(name, None, _) case</name>
  <files>src/LangBackend.Compiler/Elaboration.fs</files>
  <action>
Find the `Constructor(name, None, _)` case in `elaborateExpr` (around line 1332). Currently it unconditionally allocates a nullary 16-byte block with tag and null payload.

Modify it to check TypeEnv arity:
- If `info.Arity = 0`: keep existing nullary allocation code (unchanged)
- If `info.Arity >= 1`: wrap as Lambda and re-elaborate:
  ```fsharp
  let s = Ast.unknownSpan  // or however unknownSpan is accessed
  let paramName = sprintf "__ctor_%d_%s" env.Counter.Value name
  env.Counter.Value <- env.Counter.Value + 1
  elaborateExpr env (Ast.Lambda(paramName, Ast.Constructor(name, Some(Ast.Var(paramName, s)), s), s))
  ```

This reuses the existing Lambda closure elaboration from Phase 12 — no new closure emission code needed. The Lambda path will produce a proper closure struct with fn_ptr that, when called, allocates the ADT block with the argument as payload.

IMPORTANT: Look up `info` via `Map.find name env.TypeEnv` — the same lookup that's already used in the nullary case for the tag value. Check what field name holds the arity (likely `Arity` or `ArgTypes.Length`). Adapt the field access accordingly.

Do NOT modify any other Constructor cases. Do NOT touch MatchCompiler.fs.
  </action>
  <verify>
Run `dotnet build` in the project root — must compile without errors.
Run the full E2E test suite to confirm all 63 existing tests pass.
  </verify>
  <done>The `Constructor(name, None, _)` case branches on arity: nullary allocates block, unary+ wraps as Lambda closure.</done>
</task>

<task type="auto">
  <name>Task 2: E2E test for first-class constructor</name>
  <files>tests/e2e/20-01-firstclass-ctor.flt</files>
  <action>
Create the E2E test file `tests/e2e/20-01-firstclass-ctor.flt` with this content:

```
type Option = None | Some of int
let apply f x = f x
let _ =
  match apply Some 42 with
  | Some n -> n
  | None -> 0
```

Expected exit code: 42. The test verifies that `Some` (without argument) is passed as a first-class function to `apply`, which calls it with `42`, producing `Some 42`, which is then pattern matched to extract `42`.

Follow the existing FsLit test conventions (check a nearby test like `17-01-nullary-ctor.flt` for the exact format — no trailing newline).

Run the full test suite including the new test. Confirm all 64 tests pass (63 existing + 1 new).
  </action>
  <verify>
Run `dotnet test` or the project's E2E test runner. New test must produce exit code 42. All 64 tests pass.
  </verify>
  <done>E2E test 20-01-firstclass-ctor.flt passes with exit code 42, proving unary constructor works as first-class value.</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- All 64 tests pass (63 existing + 1 new)
- New test 20-01 exits with code 42
- Nullary constructors (None, Leaf, Red, etc.) still work in existing tests
</verification>

<success_criteria>
- `apply Some 42` compiles and produces a closure that, when called, returns `Some 42`
- Pattern match on the result extracts 42
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/20-completeness/20-01-SUMMARY.md`
</output>
