---
phase: 35-prelude-modules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Prelude/Option.fun
  - Prelude/Result.fun
  - Prelude/List.fun
  - Prelude/Array.fun
  - tests/compiler/35-05-option-module.flt
  - tests/compiler/35-06-result-module.flt
  - tests/compiler/35-07-list-module.flt
  - tests/compiler/35-08-list-tryfind-choose.flt
  - tests/compiler/35-09-array-module.flt
autonomous: true

must_haves:
  truths:
    - "Option.map, Option.bind, Option.defaultValue, Option.isSome, Option.isNone compile and return correct values"
    - "Result.map, Result.bind, Result.defaultValue, Result.toOption compile and return correct values"
    - "List.sort, List.sortBy, List.tryFind, List.choose, List.mapi, List.distinctBy, List.exists, List.item, List.isEmpty, List.head, List.tail compile and return correct values"
    - "Array.sort, Array.ofSeq, Array.ofList, Array.toList, Array.map, Array.fold compile and return correct values"
  artifacts:
    - path: "Prelude/Option.fun"
      provides: "Option ADT + utility functions (map, bind, defaultValue, iter, filter, isSome, isNone)"
    - path: "Prelude/Result.fun"
      provides: "Result ADT + utility functions (map, bind, mapError, defaultValue, toOption)"
    - path: "Prelude/List.fun"
      provides: "List module with 30+ functions including sort, tryFind, choose, mapi, distinctBy"
    - path: "Prelude/Array.fun"
      provides: "Array module wrapping array builtins"
  key_links:
    - from: "Prelude/List.fun"
      to: "type Option 'a = None | Some of 'a"
      via: "tryFind and choose return Option values — Option type must be defined in same compilation unit"
    - from: "Prelude/Result.fun"
      to: "type Option 'a = None | Some of 'a"
      via: "toOption returns Some/None — Option type must be defined"
---

<objective>
Create Prelude .fun files for Option, Result, List, and Array modules, plus E2E tests proving they compile and run correctly.

Purpose: Provide ADT types (Option, Result) and module-qualified access to list/array functions.
Output: 4 Prelude .fun files + 5 E2E .flt test files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-prelude-modules/35-RESEARCH.md

Key references:
- LangThree prelude files at ../LangThree/Prelude/ (Option.fun, Result.fun, List.fun, Array.fun)
- Phase 25 module tests at tests/compiler/25-*.flt — shows inline module definition pattern
- Phase 32 list/array builtin tests at tests/compiler/32-*.flt — confirms builtin names (list_sort_by, list_of_seq, array_sort, array_of_seq)
- Research finding: `prePassDecls` is a pre-pass that scans ALL decls before elaboration, so load order doesn't matter for type definitions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Prelude .fun files for Option, Result, List, Array</name>
  <files>Prelude/Option.fun, Prelude/Result.fun, Prelude/List.fun, Prelude/Array.fun</files>
  <action>
Create four .fun files in the `Prelude/` directory (create directory if it doesn't exist yet).

**CRITICAL naming difference from LangThree:** LangThree uses `optionMap`, `optionBind`, `resultMap`, etc. LangBackend uses short names: `map`, `bind`, `defaultValue`, etc. This matches the PRE-07/PRE-08 requirements (`Option.map`, `Result.map`).

**Prelude/Option.fun** — adapted from LangThree with SHORT function names:
```
module Option =
    type Option 'a = None | Some of 'a
    let map f opt = match opt with | Some x -> Some (f x) | None -> None
    let bind f opt = match opt with | Some x -> f x | None -> None
    let defaultValue def opt = match opt with | Some x -> x | None -> def
    let iter f opt = match opt with | Some x -> f x | None -> ()
    let filter pred opt = match opt with | Some x -> if pred x then Some x else None | None -> None
    let isSome opt = match opt with | Some _ -> true | None -> false
    let isNone opt = match opt with | Some _ -> false | None -> true
```

**Prelude/Result.fun** — adapted from LangThree with SHORT function names:
```
module Result =
    type Result 'a 'b = Ok of 'a | Error of 'b
    let map f r = match r with | Ok x -> Ok (f x) | Error e -> Error e
    let bind f r = match r with | Ok x -> f x | Error e -> Error e
    let mapError f r = match r with | Ok x -> Ok x | Error e -> Error (f e)
    let defaultValue def r = match r with | Ok x -> x | Error _ -> def
    let toOption r = match r with | Ok x -> Some x | Error _ -> None
```

NOTE: `Result.toOption` uses `Some`/`None` — the Option type must be defined in the same compilation unit. When used with CLI prelude loading, Option.fun is loaded before Result.fun. In E2E tests, Option type definition is inlined.

**Prelude/List.fun** — adapted from LangThree. Keep ALL functions from LangThree's List.fun:
Copy verbatim from `../LangThree/Prelude/List.fun`. This includes: map, filter, fold, length, reverse, append, hd, tl, zip, take, drop, any, all, flatten, nth, head, tail, exists, item, isEmpty, _insert, sort, sortBy, _mapi_helper, mapi, tryFind, choose, _distinctBy_helper, distinctBy, ofSeq.

Do NOT include `open List` at the end (no-op in LangBackend).

Do NOT include the `(++)` operator — it may conflict. Keep `append` instead.

**Prelude/Array.fun** — adapted from LangThree. Keep ALL functions:
Copy verbatim from `../LangThree/Prelude/Array.fun`: create, get, set, length, ofList, toList, iter, map, fold, init, sort, ofSeq.

Do NOT include `open Array` at the end.

Do NOT include `open List` between List and Array modules.
  </action>
  <verify>All four files exist under Prelude/ with correct content. Check that Option.fun uses short names (map, bind, defaultValue) NOT LangThree names (optionMap, optionBind, optionDefault).</verify>
  <done>Prelude/Option.fun, Prelude/Result.fun, Prelude/List.fun, Prelude/Array.fun exist with correct function names and complete function sets.</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E tests for Option, Result, List, Array modules</name>
  <files>tests/compiler/35-05-option-module.flt, tests/compiler/35-06-result-module.flt, tests/compiler/35-07-list-module.flt, tests/compiler/35-08-list-tryfind-choose.flt, tests/compiler/35-09-array-module.flt</files>
  <action>
Create five .flt test files. Each test inlines the required module definitions.

**35-05-option-module.flt** — Test Option.map, Option.bind, Option.defaultValue, Option.isSome, Option.isNone:
- Inline the Option module definition (with type Option 'a)
- Test `Option.map (fun x -> x + 1) (Some 10)` -> match result, print 11
- Test `Option.bind (fun x -> if x > 5 then Some (x * 2) else None) (Some 10)` -> match result, print 20
- Test `Option.defaultValue 0 (Some 42)` -> print 42
- Test `Option.defaultValue 0 None` -> print 0
- Test `Option.isSome (Some 1)` -> true
- Test `Option.isNone None` -> true
- Use `println (to_string ...)` for int/bool results, match for Option results
- Expected output: 11, 20, 42, 0, true, true, 0

**35-06-result-module.flt** — Test Result.map, Result.bind, Result.defaultValue, Result.toOption:
- Inline BOTH Option module (for type definition) AND Result module (uses Some/None in toOption)
- Test `Result.map (fun x -> x + 1) (Ok 10)` -> match, print 11
- Test `Result.bind (fun x -> Ok (x * 2)) (Ok 5)` -> match, print 10
- Test `Result.defaultValue 0 (Ok 42)` -> print 42
- Test `Result.defaultValue 0 (Error "fail")` -> print 0
- Test `Result.toOption (Ok 99)` -> match, print 99
- Expected output: 11, 10, 42, 0, 99, 0

**35-07-list-module.flt** — Test List basic functions: sort, map, filter, fold, length, head, tail, isEmpty, item, exists, mapi, append:
- Inline the List module definition (only the functions used — or inline all for simplicity)
- Test `List.sort [3; 1; 2]` -> print elements via for-in: 1, 2, 3
- Test `List.length [1; 2; 3]` -> 3
- Test `List.isEmpty []` -> true
- Test `List.head [10; 20]` -> 10
- Test `List.exists (fun x -> x > 2) [1; 2; 3]` -> true
- Test `List.item 1 [10; 20; 30]` -> 20
- Use `println (to_string ...)` for results, `for x in list do println (to_string x)` for list iteration
- Expected output: 1, 2, 3, 3, true, 10, true, 20, 0

**35-08-list-tryfind-choose.flt** — Test List.tryFind and List.choose (require Option type):
- Inline BOTH Option module (for type Option definition) AND List module
- Test `List.tryFind (fun x -> x > 2) [1; 2; 3; 4]` -> match, print 3
- Test `List.tryFind (fun x -> x > 10) [1; 2; 3]` -> match, print "none"
- Test `List.choose (fun x -> if x > 2 then Some x else None) [1; 2; 3; 4]` -> for-in print: 3, 4
- Expected output: 3, none, 3, 4, 0

**35-09-array-module.flt** — Test Array.ofList, Array.toList, Array.sort, Array.length, Array.map:
- Inline the Array module definition
- Test `Array.ofList [3; 1; 2]` -> sort -> toList -> for-in print: 1, 2, 3
- Test `Array.length (Array.ofList [1; 2; 3])` -> 3
- Expected output: 1, 2, 3, 3, 0

IMPORTANT NOTES:
- The List module is LARGE (30+ functions). For inline tests, you may inline the FULL module definition. The compiler handles this.
- For `List.tryFind` and `List.choose`, the Option type (`type Option 'a = None | Some of 'a`) MUST be defined before the List module in the inlined source, because `tryFind` and `choose` use `None`/`Some` constructors. Since `prePassDecls` pre-scans ALL decls, the type can be defined in any order — but to be safe, put `type Option 'a = None | Some of 'a` before the List module definition.
- Actually, since `type Option 'a` is inside `module Option = ...`, and `prePassDecls` recurses into `ModuleDecl`, it WILL be found regardless of order. So you can put Option module after List module. BUT to be readable, put it before.

Run the E2E test suite to confirm all 5 new tests pass:
```bash
dotnet test tests/LangBackend.Tests/
```
  </action>
  <verify>
Run `dotnet test tests/LangBackend.Tests/` — all tests pass including the 5 new 35-* tests.
  </verify>
  <done>E2E tests 35-05 through 35-09 pass, confirming Option, Result, List, and Array modules compile and produce correct output.</done>
</task>

</tasks>

<verification>
Run `dotnet test tests/LangBackend.Tests/` and confirm:
1. All existing 173 tests still pass (no regressions)
2. New tests 35-05 through 35-09 pass
3. Total test count is 178+ (173 existing + 5 new, possibly more if Plan 01 already added 4)
</verification>

<success_criteria>
- Option.map, Option.bind, Option.defaultValue, Option.isSome, Option.isNone compile and work
- Result.map, Result.bind, Result.defaultValue, Result.toOption compile and work
- List.sort, List.tryFind, List.choose, List.mapi, List.exists, List.item, List.isEmpty, List.head, List.tail compile and work
- Array.sort, Array.ofList, Array.toList, Array.length compile and work
- All Prelude .fun files exist at Prelude/
</success_criteria>

<output>
After completion, create `.planning/phases/35-prelude-modules/35-02-SUMMARY.md`
</output>
