---
phase: 57-unknownspan-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FunLangCompiler.Compiler/Elaboration.fs
  - src/FunLangCompiler.Cli/Program.fs
autonomous: true

must_haves:
  truths:
    - "No unknownSpan references remain in src/ directory"
    - "All 230+ existing E2E tests pass unchanged"
    - "extractMainExpr accepts moduleSpan parameter and call site is updated"
  artifacts:
    - path: "src/FunLangCompiler.Compiler/Elaboration.fs"
      provides: "10 unknownSpan replacements with real AST spans"
      contains_not: "Ast.unknownSpan"
    - path: "src/FunLangCompiler.Cli/Program.fs"
      provides: "1 unknownSpan replacement with Ast.spanOf expr"
      contains_not: "Ast.unknownSpan"
  key_links:
    - from: "extractMainExpr signature"
      to: "elaborateProgram call site (line 4450)"
      via: "moduleSpan parameter addition"
      pattern: "extractMainExpr.*moduleSpanOf"
---

<objective>
Replace all 11 unknownSpan occurrences in Elaboration.fs and Program.fs with real AST spans.

Purpose: Error messages will show correct file:line:col instead of `<unknown>:0:0:0`.
Output: Both source files modified, zero unknownSpan references remaining in src/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-unknownspan-removal/57-RESEARCH.md
@src/FunLangCompiler.Compiler/Elaboration.fs
@src/FunLangCompiler.Cli/Program.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 10 unknownSpan in Elaboration.fs</name>
  <files>src/FunLangCompiler.Compiler/Elaboration.fs</files>
  <action>
  Apply six fix patterns to remove all 10 unknownSpan occurrences in Elaboration.fs.
  Each fix binds a span variable from the surrounding pattern match instead of using unknownSpan.

  SPAN-01 (lines 2188-2202): printfn 3 desugar arms.
  - Line 2188 (2-arg): Change outer `App(..., arg2Expr, _)` to `App(..., arg2Expr, s)`, remove `let s = Ast.unknownSpan` on line 2190.
  - Line 2194 (1-arg): Change outer `App(..., argExpr, _)` to `App(..., argExpr, s)`, remove `let s = Ast.unknownSpan` on line 2196.
  - Line 2200 (0-arg): Change `App(Var("printfn", _), String(fmt, _), _)` last `_` to `s`, remove `let s = Ast.unknownSpan` on line 2202.

  SPAN-02 (line 2297-2299): eprintfn desugar.
  - Change `App(Var("eprintfn", _), String(fmt, _), _)` last `_` to `s`, remove `let s = Ast.unknownSpan` on line 2299.

  SPAN-03 (line 1498-1500): show builtin string literal path.
  - Change `App(Var("show", _), String(s, _), _)` to `App(Var("show", _), String(s, _), appSpan)`.
  - Change `elaborateExpr env (Ast.String(s, Ast.unknownSpan))` to `elaborateExpr env (Ast.String(s, appSpan))`.

  SPAN-04 (lines 1526-1529): eq builtin string literal path.
  - Change `App(App(Var("eq", _), String(ls, _), _), String(rs, _), _)` to `App(App(Var("eq", _), String(ls, lsSpan), _), String(rs, rsSpan), _)`.
  - Replace `Ast.String(ls, Ast.unknownSpan)` with `Ast.String(ls, lsSpan)`.
  - Replace `Ast.String(rs, Ast.unknownSpan)` with `Ast.String(rs, rsSpan)`.

  SPAN-05 (line 680, 798): Closure capture failure.
  - On line 680, change the trailing `_` in `Let(name, StripAnnot(Lambda(...)), inExpr, _)` to `letSpan`.
  - On line 798, change `failWithSpan Ast.unknownSpan` to `failWithSpan letSpan`.
  - WARNING: Do NOT try to bind Lambda span inside StripAnnot -- it's an active pattern, not a constructor. Bind the outer Let's 4th field.

  SPAN-06 (line 3146, 3153): First-class constructor wrapping.
  - Change `Constructor(name, None, _)` to `Constructor(name, None, ctorSpan)` on line 3146.
  - Change `let s = Ast.unknownSpan` to `let s = ctorSpan` on line 3153.

  SPAN-07 (line 4313-4314, 4450): extractMainExpr signature change.
  - Change signature from `let private extractMainExpr (decls: Ast.Decl list) : Expr =` to `let private extractMainExpr (moduleSpan: Ast.Span) (decls: Ast.Decl list) : Expr =`.
  - Change `let s = unknownSpan` to `let s = moduleSpan` on line 4314.
  - Update call site on line 4450: change `extractMainExpr decls` to `extractMainExpr (Ast.moduleSpanOf ast) decls`.
  </action>
  <verify>
  Run: `grep -n "unknownSpan" src/FunLangCompiler.Compiler/Elaboration.fs` -- should return 0 results.
  Run: `dotnet build` from project root -- should compile without errors.
  </verify>
  <done>All 10 unknownSpan occurrences in Elaboration.fs replaced with real spans. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Replace unknownSpan in Program.fs</name>
  <files>src/FunLangCompiler.Cli/Program.fs</files>
  <action>
  SPAN-08 (line 51): parseExpr fallback wrapping.
  - After `let expr = parseExpr src filename` (line 50), add `let exprSpan = Ast.spanOf expr`.
  - Change `Ast.Module([Ast.Decl.LetDecl("_", expr, Ast.unknownSpan)], Ast.unknownSpan)` to `Ast.Module([Ast.Decl.LetDecl("_", expr, exprSpan)], exprSpan)`.

  Both unknownSpan usages on line 51 must be replaced.
  </action>
  <verify>
  Run: `grep -n "unknownSpan" src/FunLangCompiler.Cli/Program.fs` -- should return 0 results.
  Run: `grep -rn "unknownSpan" src/` -- should return 0 results total across all source files.
  Run: `dotnet build` -- should compile without errors.
  </verify>
  <done>Program.fs unknownSpan removed. Zero unknownSpan references in entire src/ directory. Full build succeeds.</done>
</task>

</tasks>

<verification>
1. `grep -r "unknownSpan" src/` returns 0 results.
2. `dotnet build` succeeds with no errors or warnings related to our changes.
3. Run full E2E test suite: `dotnet run --project tests/FsLit/FsLit.fsproj -- tests/compiler/` -- all 230+ tests pass.
</verification>

<success_criteria>
- Zero unknownSpan in src/ (grep verification)
- Build compiles cleanly
- All existing E2E tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/57-unknownspan-removal/57-01-SUMMARY.md`
</output>
