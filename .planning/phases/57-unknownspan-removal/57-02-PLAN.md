---
phase: 57-unknownspan-removal
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - tests/compiler/57-01-unknownspan-removed.flt
  - tests/compiler/57-01-unknownspan-removed.fun
autonomous: true

must_haves:
  truths:
    - "E2E test confirms no 0:0 positions in error output"
    - "Grep verification proves zero unknownSpan in src/"
    - "All 230+ existing tests still pass"
  artifacts:
    - path: "tests/compiler/57-01-unknownspan-removed.flt"
      provides: "E2E test verifying error spans are real"
    - path: "tests/compiler/57-01-unknownspan-removed.fun"
      provides: "FunLang source triggering an error with span"
  key_links:
    - from: "tests/compiler/57-01-unknownspan-removed.fun"
      to: "Elaboration.fs error paths"
      via: "FunLang source that triggers elaboration error"
      pattern: "CHECK-RE:.*\\d+:\\d+"
---

<objective>
Create E2E tests verifying that unknownSpan removal produces real source locations in error messages.

Purpose: Prove the span fixes work end-to-end and prevent regression.
Output: New test files + full test suite passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-unknownspan-removal/57-RESEARCH.md
@.planning/phases/57-unknownspan-removal/57-01-SUMMARY.md
@tests/compiler/46-01-record-type-hint.flt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E tests for span accuracy</name>
  <files>
    tests/compiler/57-01-unknownspan-removed.flt
    tests/compiler/57-01-unknownspan-removed.fun
  </files>
  <action>
  Create E2E tests that verify real spans appear in error messages. Focus on testable error paths identified in research:

  1. First, run `grep -r "unknownSpan" src/` to confirm Plan 01 completed (0 results expected).

  2. Create a test that triggers an elaboration error where the span previously would have been unknownSpan. The best candidate is a construct that exercises the first-class constructor path (SPAN-06) or another error path that produces a visible span in stderr.

  Research notes indicate SPAN-05 (closure capture) is an internal invariant -- hard to trigger from user code. Focus on paths where a user-visible error naturally fires.

  Test approach: Write a `.fun` file that triggers an elaboration error in one of the fixed paths. The `.flt` file uses `CHECK-RE:` to verify the error message includes a real file:line:col (not `0:0`).

  Follow the exact test format from existing tests (e.g., `46-01-record-type-hint.flt`):
  ```
  // Test: <description>
  // --- Command: bash -c 'cd %S && dotnet run --project ../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- 57-01-unknownspan-removed.fun 2>&1; echo $?'
  // --- Input:
  // --- Output:
  CHECK-RE: <pattern matching real span>
  ```

  Also create a grep-based verification test if feasible, or simply verify via the test runner that no `0:0:0` positions appear.

  3. Run the full test suite to verify all tests pass: `dotnet run --project tests/FsLit/FsLit.fsproj -- tests/compiler/`
  </action>
  <verify>
  Run: `dotnet run --project tests/FsLit/FsLit.fsproj -- tests/compiler/57-01-unknownspan-removed.flt` -- new test passes.
  Run: `dotnet run --project tests/FsLit/FsLit.fsproj -- tests/compiler/` -- full suite passes (230+ tests).
  Run: `grep -r "unknownSpan" src/` -- returns 0 results.
  </verify>
  <done>E2E test created and passing. Full test suite passes. Zero unknownSpan in src/. Phase 57 complete.</done>
</task>

</tasks>

<verification>
1. `grep -r "unknownSpan" src/` returns 0 results.
2. All existing 230+ E2E tests pass.
3. New 57-01 test passes and verifies real file:line:col in error output.
4. No `0:0` positions appear in error test outputs.
</verification>

<success_criteria>
- Zero unknownSpan in src/ (grep verification)
- All E2E tests pass including new 57-01 test
- Error messages show real source positions (non-zero line:col)
</success_criteria>

<output>
After completion, create `.planning/phases/57-unknownspan-removal/57-02-SUMMARY.md`
</output>
