---
phase: 58-namespace-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FunLangCompiler.Compiler/Elaboration.fs
  - src/FunLangCompiler.Cli/Program.fs
autonomous: true

must_haves:
  truths:
    - "dotnet build succeeds with 0 errors"
    - "No NamespaceDecl or NamespacedModule references remain in src/"
    - "All 232 E2E tests pass without regression"
  artifacts:
    - path: "src/FunLangCompiler.Compiler/Elaboration.fs"
      provides: "Elaboration without NamespaceDecl/NamespacedModule patterns"
      contains_none: ["NamespaceDecl", "NamespacedModule"]
    - path: "src/FunLangCompiler.Cli/Program.fs"
      provides: "CLI entry point without NamespaceDecl/NamespacedModule patterns"
      contains_none: ["NamespaceDecl", "NamespacedModule"]
  key_links:
    - from: "Elaboration.fs pattern matches"
      to: "FunLang Ast DU cases"
      via: "match expressions must cover only existing DU cases"
      pattern: "Ast\\.Decl\\.|Ast\\.Module\\(|Ast\\.NamedModule"
---

<objective>
Remove all NamespaceDecl and NamespacedModule references from the Compiler codebase to restore build after FunLang removed these AST types.

Purpose: FunLang submodule update deleted NamespaceDecl from Ast.Decl and NamespacedModule from Ast.Module, breaking the Compiler build. This plan removes all dead references.
Output: Clean build, all 232 E2E tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/FunLangCompiler.Compiler/Elaboration.fs
@src/FunLangCompiler.Cli/Program.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove NamespaceDecl/NamespacedModule from Elaboration.fs</name>
  <files>src/FunLangCompiler.Compiler/Elaboration.fs</files>
  <action>
Remove all NamespaceDecl and NamespacedModule references in Elaboration.fs:

1. **prePassDecls (line ~4237-4238):** The or-pattern `| Ast.Decl.ModuleDecl(_, innerDecls, _) | Ast.Decl.NamespaceDecl(_, innerDecls, _) ->` — remove the NamespaceDecl alternative, keeping only `| Ast.Decl.ModuleDecl(_, innerDecls, _) ->` with the same body.

2. **flattenDecls (line ~4283):** Remove the entire line `| Ast.Decl.NamespaceDecl(_, innerDecls, _) -> flattenDecls moduleMembers modName innerDecls`.

3. **elaborateTypeclasses (lines ~4380-4381):** Remove the entire case:
   ```
   | Ast.Decl.NamespaceDecl(path, innerDecls, span) ->
       [Ast.Decl.NamespaceDecl(path, elaborateTypeclasses innerDecls, span)]
   ```

4. **elaborateProgram (line ~4442):** Change the or-pattern from `| Ast.Module(decls, _) | Ast.NamedModule(_, decls, _) | Ast.NamespacedModule(_, decls, _) -> decls` to `| Ast.Module(decls, _) | Ast.NamedModule(_, decls, _) -> decls`.

5. **Comments cleanup:**
   - Line ~4272: Change "flatten ModuleDecl/NamespaceDecl" to "flatten ModuleDecl"
   - Line ~4306: Change "Flattens ModuleDecl/NamespaceDecl" to "Flattens ModuleDecl"
   - Line ~4354: Remove the entire comment line "// - NamespaceDecl: recurse into inner decls"
  </action>
  <verify>
Run: `grep -n "NamespaceDecl\|NamespacedModule" src/FunLangCompiler.Compiler/Elaboration.fs` — expect 0 results.
  </verify>
  <done>Zero references to NamespaceDecl or NamespacedModule in Elaboration.fs.</done>
</task>

<task type="auto">
  <name>Task 2: Remove NamespaceDecl/NamespacedModule from Program.fs</name>
  <files>src/FunLangCompiler.Cli/Program.fs</files>
  <action>
Remove all NamespaceDecl and NamespacedModule references in Program.fs:

1. **expandImports extracted decls (line ~90):** Change `| Ast.Module(ds, _) | Ast.NamedModule(_, ds, _) | Ast.NamespacedModule(_, ds, _) -> ds` to `| Ast.Module(ds, _) | Ast.NamedModule(_, ds, _) -> ds`.

2. **expandImports NamespaceDecl case (lines ~97-98):** Remove the entire case:
   ```
   | Ast.Decl.NamespaceDecl(name, innerDecls, s) ->
       [Ast.Decl.NamespaceDecl(name, expandImports visitedFiles currentFile innerDecls, s)]
   ```

3. **preludeDecls extraction (line ~184):** Change `| Ast.Module(ds, _) | Ast.NamedModule(_, ds, _) | Ast.NamespacedModule(_, ds, _) -> ds` to `| Ast.Module(ds, _) | Ast.NamedModule(_, ds, _) -> ds`.

4. **userDecls extraction (line ~189):** Change `| Ast.Module(ds, s) | Ast.NamedModule(_, ds, s) | Ast.NamespacedModule(_, ds, s) -> (ds, s)` to `| Ast.Module(ds, s) | Ast.NamedModule(_, ds, s) -> (ds, s)`.

5. **expandedAst NamespacedModule case (lines ~202-205):** Remove the entire case block:
   ```
   | Ast.NamespacedModule(nm, ds, s) ->
       let visited = System.Collections.Generic.HashSet<string>()
       visited.Add(System.IO.Path.GetFullPath(inputPath)) |> ignore
       Ast.NamespacedModule(nm, expandImports visited inputPath ds, s)
   ```

6. **tcAst NamespacedModule case (line ~212):** Remove the entire line:
   `| Ast.NamespacedModule(p, ds, s) -> Ast.NamespacedModule(p, Elaboration.elaborateTypeclasses ds, s)`
  </action>
  <verify>
Run: `grep -n "NamespaceDecl\|NamespacedModule" src/FunLangCompiler.Cli/Program.fs` — expect 0 results.
  </verify>
  <done>Zero references to NamespaceDecl or NamespacedModule in Program.fs.</done>
</task>

<task type="auto">
  <name>Task 3: Build and test</name>
  <files></files>
  <action>
1. Run `dotnet build` from project root — must succeed with 0 errors.
2. Run the E2E test suite — all 232 tests must pass.
3. Run final grep: `grep -rn "NamespaceDecl\|NamespacedModule" src/ --include="*.fs"` — must return 0 results.
  </action>
  <verify>
- `dotnet build` exits 0 with "Build succeeded" and 0 errors
- E2E tests: 232 passed, 0 failed
- grep returns empty
  </verify>
  <done>Build succeeds, all 232 E2E tests pass, no NamespaceDecl/NamespacedModule references in src/.</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds (0 errors, 0 warnings related to namespace)
2. `grep -rn "NamespaceDecl\|NamespacedModule" src/ --include="*.fs"` returns empty
3. Full E2E test suite passes (232 tests)
</verification>

<success_criteria>
- Build compiles cleanly
- All 232 E2E tests pass
- Zero NamespaceDecl/NamespacedModule references in src/
</success_criteria>

<output>
After completion, create `.planning/phases/58-namespace-removal/58-01-SUMMARY.md`
</output>
