---
phase: 65-partial-env-pattern-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FunLangCompiler.Compiler/Elaboration.fs
autonomous: true

must_haves:
  truths:
    - "Definition site emits GC_malloc + fn_ptr store + non-outerParam capture stores when hasNonOuterCaptures is true"
    - "Template env ptr is stored in env.Vars[name] so LetRec bodies can find it"
    - "Call site fallback clones template env, fills outerParam slot, and calls inner function via IndirectCallOp"
    - "Fast path (all captures in scope) remains unchanged"
    - "Functions with no non-outerParam captures skip template allocation entirely"
  artifacts:
    - path: "src/FunLangCompiler.Compiler/Elaboration.fs"
      provides: "Definition-site partial env allocation + template-copy call path"
  key_links:
    - from: "2-lambda Let handler (line ~852-874)"
      to: "KnownFuncs call site fallback (line ~2644-2659)"
      via: "env.Vars[name] carries template env ptr from definition to call site"
---

<objective>
Implement the partial env pattern in Elaboration.fs: (1) at the 2-lambda definition site, pre-allocate a template env with fn_ptr + non-outerParam captures stored, and (2) at the KnownFuncs call site fallback, clone the template env, fill outerParam, and call the inner function.

Purpose: Fix Issue #5 crash where LetRec body calls 3+ arg curried functions with outer captures.
Output: Modified Elaboration.fs with definition-site env allocation and template-copy call path.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-partial-env-pattern-implementation/65-RESEARCH.md
@src/FunLangCompiler.Compiler/Elaboration.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Definition-site template env allocation + template-copy call path</name>
  <files>src/FunLangCompiler.Compiler/Elaboration.fs</files>
  <action>
Two changes to Elaboration.fs:

**Change A: Definition-site template env (after Step 6, before Step 7, around line 872-874)**

After `let env' = ...` (the env with KnownFuncs updated, line 868-871), and BEFORE `elaborateExpr env' inExpr` (line 874), insert the partial env allocation logic:

1. Compute `let hasNonOuterCaptures = captures |> List.exists (fun c -> c <> outerParam)`
2. If `hasNonOuterCaptures` is true:
   - Emit `ArithConstantOp` for `(numCaptures + 1) * 8` bytes
   - Emit `LlvmCallOp` to `@GC_malloc` to allocate the template env
   - Emit `LlvmAddressOfOp` to get fn_ptr for `closureFnName`
   - Emit `LlvmStoreOp` to store fn_ptr at env slot 0
   - For each capture in `captures` where `capName <> outerParam`: look up `capName` in `env.Vars` (the OUTER env, not env'). If found, emit `LlvmGEPLinearOp` to slot `i + 1`, then store the capture value (with `LlvmIntToPtrOp` coercion if type is I64).
   - Store the template env ptr in env'.Vars: `let envWithTemplateVar = { env' with Vars = Map.add name tEnvPtr env'.Vars }`
   - Collect all emitted ops into `templateEnvOps`
3. If `hasNonOuterCaptures` is false: `templateEnvOps = []`, `envWithTemplateVar = env'`

Use `freshName env` for all new SSA values (use the OUTER env's counter, not innerEnv's).

4. Change Step 7 from:
   ```fsharp
   elaborateExpr env' inExpr
   ```
   to:
   ```fsharp
   let (inVal, inOps) = elaborateExpr envWithTemplateVar inExpr
   (inVal, templateEnvOps @ inOps)
   ```

**Change B: Template-copy call path (replace fallback at lines 2644-2659)**

Replace the `else` branch (lines 2644-2659) where `captureStoreResult` has at least one `None`. The current code tries `Map.tryFind name env.Vars` and does a broken indirect call. Replace with:

1. `match Map.tryFind name env.Vars with`
2. `| Some templatePtr ->` (template env exists from definition site):
   - Emit `ArithConstantOp` for `(ci.NumCaptures + 1) * 8` bytes
   - Emit `LlvmCallOp` to `@GC_malloc` to allocate a NEW env
   - Copy ALL slots `[0 .. ci.NumCaptures]` from templatePtr to newEnvPtr using `LlvmGEPLinearOp` + `LlvmLoadOp` + `LlvmGEPLinearOp` + `LlvmStoreOp` per slot
   - Compute outerParam slot index: `List.tryFindIndex ((=) ci.OuterParamName) ci.CaptureNames |> Option.get` then add 1 (slot 0 = fn_ptr)
   - Overwrite outerParam slot in newEnvPtr: `LlvmGEPLinearOp` to the slot, `LlvmIntToPtrOp` to coerce `i64ArgVal` to Ptr, `LlvmStoreOp`
   - Load fn_ptr from newEnvPtr slot 0: `LlvmLoadOp`
   - Call via `IndirectCallOp(resultVal, fnPtrVal, newEnvPtr, argVal)` where argVal is the ORIGINAL argVal (not i64ArgVal) -- actually check: IndirectCallOp expects (result, fnPtr, envPtr, arg). The arg should match what the inner llvm.func expects (Ptr). Use `argVal` from `elaborateExpr env argExpr`.
   - Return `(resultVal, argOps @ setupOps @ coerceOps @ copyOps @ slotCopyOps @ outerStoreOps @ callOps)`
   - Note: `setupOps` (GC_malloc for the original env) are NOT needed in this path -- we allocate our own new env. So skip the original `setupOps` entirely. The return should be `(resultVal, argOps @ coerceOps @ newAllocOps @ slotCopyOps @ outerStoreOps @ [loadFnPtr; callOp])`
   - Wait -- re-examine the code structure. The `setupOps` and `coerceOps` are computed BEFORE the if/else branch. The `setupOps` allocates `envPtrVal` which is only used in the fast path. In the template path, we allocate our own `newEnvPtr`. So the template path should NOT include `setupOps`. But `coerceOps` (which computes `i64ArgVal` from `argVal`) IS needed for the outerParam store. Structure the return as: `(resultVal, argOps @ coerceOps @ [newBytesOp; newMallocOp] @ slotCopyOps @ outerStoreOps @ [loadOp; callOp])`
   - IMPORTANT: The result type should be `Ptr` (not I64) since closure-making calls return Ptr (the env pointer). Check: `resultVal` is declared as `{ Name = freshName env; Type = Ptr }` at line 2606. But in the template path we're doing IndirectCallOp which returns I64 (inner function return). Actually wait -- re-read the context. This is the CLOSURE-MAKING call (first application, `f arg` returns a closure). The result should be the new env pointer, not the inner function result. So: store outerParam via maker logic, then return newEnvPtr as the result. Actually, we need to call the MAKER function which stores fn_ptr + outerParam. But we already have fn_ptr in the template, and we're storing outerParam ourselves. So we can skip calling the maker entirely and just return newEnvPtr directly as the closure. The maker's job was: (1) store fn_ptr at slot 0, (2) store outerParam at its slot, (3) return env ptr. We do all of that with the template copy + outerParam fill. So: `(resultVal_as_ptr, argOps @ coerceOps @ allocOps @ slotCopyOps @ outerStoreOps)` where resultVal is the newEnvPtr (type Ptr). No IndirectCallOp needed -- we ARE the maker.
   - Correction: DO NOT call IndirectCallOp. The closure-making call site returns a closure (Ptr to env). The template-copy path replaces the maker call by: clone template, fill outerParam, return newEnvPtr. So: `(newEnvPtr, argOps @ coerceOps @ allocOps @ slotCopyOps @ outerStoreOps)`
3. `| None ->` keep the existing failwith error message.

**Key correctness points:**
- In Change A, use `env.Vars` (outer env) to look up capture values, NOT `env'.Vars` (which has KnownFuncs but same Vars as env).
- In Change B, the result is `newEnvPtr` (type Ptr), matching the existing `resultVal` type.
- In Change B, `argOps` are needed (to evaluate the argument), `coerceOps` are needed (to get i64ArgVal for outerParam store), but `setupOps` are NOT needed (we do our own malloc).
  </action>
  <verify>
Run `dotnet build src/FunLangCompiler.Cli` to confirm compilation succeeds. Then run:
- `bash tests/compiler/64-01-3arg-outer-capture.sh` (should print 36)
- `bash tests/compiler/64-02-3arg-letrec.sh` (should print 15)
  </verify>
  <done>
Elaboration.fs compiles without errors. Both existing Phase 64 tests pass. The definition-site template env allocation is emitted only when hasNonOuterCaptures is true. The call site fallback uses template-copy instead of broken indirect call.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full E2E test suite to verify no regressions</name>
  <files></files>
  <action>
Run the full E2E test suite:
```bash
dotnet run --project deps/fslit/FsLit/FsLit.fsproj -- tests/compiler/
```
All 246 tests must pass. If any test fails, diagnose and fix the issue in Elaboration.fs before proceeding.

Common failure modes to watch for:
- Functions with no outer captures accidentally getting template env (check `hasNonOuterCaptures` guard)
- Template env ops emitted in wrong order (must be before inExpr ops)
- Fast path accidentally entering template-copy path (check `List.forall Option.isSome` guard)
  </action>
  <verify>Full test suite output shows all tests passing with 0 failures.</verify>
  <done>All 246 existing E2E tests pass with 0 regressions.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/FunLangCompiler.Cli` succeeds
2. `bash tests/compiler/64-01-3arg-outer-capture.sh` prints `36`
3. `bash tests/compiler/64-02-3arg-letrec.sh` prints `15`
4. `dotnet run --project deps/fslit/FsLit/FsLit.fsproj -- tests/compiler/` — all tests pass
</verification>

<success_criteria>
- Elaboration.fs has definition-site template env allocation for 2-lambda functions with non-outerParam captures
- Elaboration.fs has template-copy call path replacing the broken indirect fallback
- All 246 E2E tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/65-partial-env-pattern-implementation/65-01-SUMMARY.md`
</output>
