---
phase: 89-c-runtime-adaptation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FunLangCompiler.Compiler/lang_runtime.c
  - src/FunLangCompiler.Compiler/Elaboration.fs
autonomous: true

must_haves:
  truths:
    - "LANG_TAG_INT and LANG_UNTAG_INT macros defined in lang_runtime.c"
    - "lang_array_init passes LANG_TAG_INT(i) to closure"
    - "lang_for_in_hashtable constructs tuple with LANG_TAG_INT(e->key)"
    - "lang_for_in_hashset passes LANG_TAG_INT(e->key) to closure"
    - "lang_hashtable_trygetvalue stores tagged bool (3/1) and tagged zero placeholder"
    - "lang_hashtable_trygetvalue_str stores tagged bool (3/1)"
    - "All 257 E2E tests pass"
  artifacts:
    - path: "src/FunLangCompiler.Compiler/lang_runtime.c"
      provides: "Tagged C runtime functions"
      contains: "LANG_TAG_INT"
---

<objective>
Fix 5 C runtime functions to tag integer values at the C↔FunLang boundary.

C functions that pass integers to FunLang closures or store integers in compiler-read tuples
must use tagged representation (2n+1). This fixes the 6 remaining test failures from Phase 88.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add TAG macros and fix 5 C functions</name>
  <files>src/FunLangCompiler.Compiler/lang_runtime.c</files>
  <action>
1. Add tagging macros near the top of lang_runtime.c (after includes):
```c
#define LANG_TAG_INT(n)    (((int64_t)(n) << 1) | 1)
#define LANG_UNTAG_INT(v)  ((int64_t)(v) >> 1)
```

2. Fix `lang_array_init`: Change `fn(closure, i)` to `fn(closure, LANG_TAG_INT(i))`

3. Fix `lang_for_in_hashtable`: Change `tup[0] = e->key` to `tup[0] = LANG_TAG_INT(e->key)`

4. Fix `lang_for_in_hashset`: Change `fn(closure, e->key)` to `fn(closure, LANG_TAG_INT(e->key))`

5. Fix `lang_hashtable_trygetvalue`:
   - Found case: `tup[0] = 1` → `tup[0] = LANG_TAG_INT(1)` (tagged true = 3)
   - Not found case: `tup[0] = 0` → `tup[0] = LANG_TAG_INT(0)` (tagged false = 1)
   - Not found placeholder: `tup[1] = 0` → `tup[1] = LANG_TAG_INT(0)` (tagged zero = 1)

6. Fix `lang_hashtable_trygetvalue_str`: Same bool fix as above for tup[0] in both branches + tup[1] placeholder.
  </action>
  <verify>
1. `dotnet build src/FunLangCompiler.Cli` succeeds
2. `dotnet run --project deps/fslit/FsLit/FsLit.fsproj -- tests/compiler/` — check previously failing tests
  </verify>
  <done>5 C functions tag values at boundary. LANG_TAG_INT/LANG_UNTAG_INT macros defined.</done>
</task>

<task type="auto">
  <name>Task 2: Debug test 66-06 and fix remaining failures</name>
  <files>src/FunLangCompiler.Compiler/Elaboration.fs, src/FunLangCompiler.Compiler/lang_runtime.c</files>
  <action>
After Task 1, run full test suite. If 66-06 still fails, investigate:
1. Run `bash tests/compiler/66-06-file-exists-coerce.sh` with stderr to see error
2. Trace the file_exists → not → if path
3. Fix the root cause (likely compiler-side closure/branching issue)

Also audit: queue_enqueue, mutablelist_add — verify values stored tagged.
  </action>
  <verify>`dotnet run --project deps/fslit/FsLit/FsLit.fsproj -- tests/compiler/` — ALL tests pass (257/257)</verify>
  <done>All 257 E2E tests pass. Phase 89 complete.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/FunLangCompiler.Cli` — no errors
2. Previously failing tests pass:
   - tests/compiler/24-04-array-init.flt
   - tests/compiler/32-01-hashtable-trygetvalue.flt
   - tests/compiler/34-05-forin-tuple-ht.flt
   - tests/compiler/34-06-forin-hashset.flt
   - tests/compiler/35-02-hashtable-module.flt
   - tests/compiler/66-06-file-exists-coerce.flt
3. Full suite: 257/257
</verification>
