// --- Command: bash -c 'OUT=$(mktemp /tmp/fnc_XXXXXX); dotnet run --project %S/../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- --strict-typecheck %input -o $OUT >/dev/null 2>&1; ec=$?; sz=$(wc -c < $OUT); rm -f $OUT; echo $ec $sz'
// --- Input:
let f (x : int) : string = x + 1
let _ = printfn "%s" (f 5)
// --- Output:
1 0
