// --- Command: bash -c 'PROJROOT=%S/../..; D=$(mktemp -d /tmp/fnc_XXXXXX); ln -s "$PROJROOT/Prelude" "$D/Prelude"; echo "let _ = println (to_string (char_to_int '"'"'Z'"'"'))" > "$D/t.fun"; OUTBIN=$(mktemp /tmp/fnc_XXXXXX); dotnet run --project "$PROJROOT/src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj" -- "$D/t.fun" -o "$OUTBIN" 2>/dev/null && chmod +x "$OUTBIN" && "$OUTBIN"; echo $?; rm -rf "$D" "$OUTBIN"'
// --- Input:
// --- Output:
90
0
