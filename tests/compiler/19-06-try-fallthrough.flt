// --- Command: bash -c 'OUTBIN=$(mktemp /tmp/fnc_XXXXXX) && dotnet run --project %S/../../src/FunLangCompiler.Cli/FunLangCompiler.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN 2>&1; echo $?; rm -f $OUTBIN'
// --- Input:
exception Failure of string
exception NotFound
let _ =
  try raise (NotFound)
  with
  | Failure msg -> 0
// --- Output:
Fatal: unhandled exception
1
