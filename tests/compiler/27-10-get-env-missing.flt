// --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/LangBackend.Cli/LangBackend.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; RC=$?; rm -f $OUTBIN; echo $RC'
// --- Input:
let result = try get_env "NONEXISTENT_VAR_27" with e -> "caught" in println result
// --- Output:
caught
0
