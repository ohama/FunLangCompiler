// --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/LangBackend.Cli/LangBackend.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; echo $?; rm -f $OUTBIN'
// --- Input:
let a = string_startswith "hello.fun" "hello" in
let _ = println (to_string a) in
let b = string_startswith "hello.fun" "world" in
println (to_string b)
// --- Output:
true
false
0
