// Test: Generic type variables in annotations (LangThree v8.0)
// --- Command: bash -c 'OUTBIN=$(mktemp /tmp/langback_XXXXXX) && dotnet run --project %S/../../src/LangBackend.Cli/LangBackend.Cli.fsproj -- %input -o $OUTBIN && $OUTBIN; echo $?; rm -f $OUTBIN'
// --- Input:
let id (x : 'a) : 'a = x
let const_ (x : 'a) (y : 'b) : 'a = x
let _ = println (to_string (id 42))
let _ = println (id "hello")
let _ = println (to_string (const_ 99 "ignored"))
// --- Output:
42
hello
99
0
