// Test: nested control flow — while+if-then, while+if-else, nested while, while+&&, while>if>while
// Regression test for issue #18 (if-then continuation dead code)
// --- Command: bash %S/93-04-nested-control-flow.sh
// --- Input:
// --- Output:
hit
3
9
6
3
22
12
